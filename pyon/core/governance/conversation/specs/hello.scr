package ooi.hello;
type <python> "string" from "Lib/string.py" as string;

global protocol Hello(role requester, role provider)
{   hello() from requester to provider;
    (hello:string) from provider to requester;

    how_are_you() from requester to provider;
    (fine:string) from provider to requester;
}