local protocol RPC at requester(role provider, role requester)
{
    request() to provider;
    choice at provider {
        accept() from provider;
        () from provider;}
    } or {
        reject() to provider;}}