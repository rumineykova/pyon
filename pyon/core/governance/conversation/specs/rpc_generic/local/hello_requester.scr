package hello_requester;

local protocol Hello_requester at requester (role requester, role provider)
{
hello() to provider;
(fine:string) from provider;
how_are_you() to provider;
(howareyou:string) from provider;
}

